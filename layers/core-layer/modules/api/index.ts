import { defineNuxtModule, createResolver,  } from '@nuxt/kit'

export default defineNuxtModule({
  setup (options, nuxt) {
    // const roptions = useRuntimeConfig().public
    // console.log(import.meta.url);
    // console.log(options)
    // console.log(roptions)
    // console.log(nuxt.options);
  }
})